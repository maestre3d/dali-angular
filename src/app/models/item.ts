export class Item {
    constructor(
        public _id: string,
        public name: string,
        public brand: string,
        public type: string,
        public stock: number,
        public price: number
    ) {
    }
}
